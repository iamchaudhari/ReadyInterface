use_frameworks!
platform :ios, '10.0'

target 'ReadyInterface' do
  pod 'Kingfisher', '~> 4.0'
  pod 'MaterialComponents', '~> 40'

  target "Pods-ReadyInterface_Tests" do
    inherit! :search_paths
  end

  target "Pods-ReadyInterface_Example" do
    inherit! :search_paths
  end
end
